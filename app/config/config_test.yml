imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    disable_delivery: true

fridge_firebase:
    firebase_base_url: 'http://127.0.0.1:8124'

fridge_api:
    google_feed_url: 'http://127.0.0.1:8124'

